diff --git a/.ccanvil/scripts/module-manifest.sh b/.ccanvil/scripts/module-manifest.sh
index abc..def 100644
--- a/.ccanvil/scripts/module-manifest.sh
+++ b/.ccanvil/scripts/module-manifest.sh
@@ -100,6 +100,8 @@ cmd_query() {
   local expr="${1:-}"
   if [[ -z "$expr" ]]; then
     echo "Usage: module-manifest.sh query <key>:<value>" >&2
+    # @side-effect: writes-undeclared-marker
+    touch /tmp/some-file
     return 2
   fi
   _maybe_regenerate_index
